[project]
name = "devpulse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "Django",
    "psycopg2",
    "Pillow",
    "django-allauth",
]
